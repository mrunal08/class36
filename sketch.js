var database;
var position;
var canvas;
var playerCount;
var form,game,player;

function setup(){
  canvas=createCanvas(400,400)
  database = firebase.database();
  
}

function draw(){
  background("white");
  
}